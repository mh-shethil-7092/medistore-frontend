// this is next.config.ts, not next.config.js, so we can use TypeScript features and type checking
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
